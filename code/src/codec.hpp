#include <string>
#include <vector>

#include <cstdint>

using namespace std;

namespace Client
{
	string Decode( const string &s );

	string Encode( const char c );

	string Encode( const string &s );
}